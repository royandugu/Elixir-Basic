defmodule Sys do
  def check do
    IO.puts "Given is an #{is_atom (:Pottasium)}"
    #Here basically potassium is like potassium=potassium which is an atom
    one_to_ten=1..10
  end
end
Sys.check()
